    // Exercicio: Faça um algoritmo para ler um número inteiro, armazenar em uma variável, calcular o dobro, e exibir;
    const vetor = [];
    vetor [0] = 12;

    console.log(vetor[0]);

    let vetorTextos = [0];
    vetorTextos[0] = "doze"
    console.log(vetorTextos)

    // Exercicio: Faça um algoritmo para ler três números inteiros, armanezar em variáveis, exibir a soma, a subtraçã, a multiplicação
    // e a média dos trÊs números digitados.

    const vetor = [];
    vetor [0] = 12;
    vetor [1] = 15;
    vetor [2] = 20;

    let vetorTextos = [0]; [1]; [2];
    vetorTextos[0] = "Doze";
    vetorTextos[1] = "Quinze";
    vetorTextos[2] = "Vinte";
    console.log(vetorTextos)

    //Soma
    let soma = vetor[0] + vetor[1] + vetor[2];
    //Exibir o resultado da soma:
    console.log("A soma dos três números é: " + soma);

    //Multiplicação
    let multiplicação = vetor[0] * vetor[1] * vetor[2];
    //Exibir o resultado da multipliacação:
    console.log("A multiplicação dos três números é: " + multiplicação);

    //Subtração
    let subtração = vetor[0] - vetor[1] - vetor[2];
    //Exibir o resultado da subtração:
    console.log("A subtração dos três números é: " + subtração);

    //Média
    let média = vetor[0] + vetor[1] + vetor[2];
    //Exibir a média de valores:
    console.log("A média dos 3 números é: " + média);


    // Faça um algoritmo para ler duas notas, e calcular a média:
    const vetor = [];
    vetor[1] = 10;
    vetor[2] = 5;
    //Exibir a média das notas: 
    let média = vetor[1] + vetor[2] / 2;
    console.log("A média das suas notas é: " + média);







